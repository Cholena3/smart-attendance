{
  "name": "smart-attendance",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix teacher-app\" \"npm run dev --prefix student-app\"",
    "install-all": "npm install --prefix server && npm install --prefix teacher-app && npm install --prefix student-app"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
